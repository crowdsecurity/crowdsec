// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/crowdsecurity/crowdsec/pkg/database/ent/apicauth"
	"github.com/crowdsecurity/crowdsec/pkg/database/ent/predicate"
)

// ApicAuthQuery is the builder for querying ApicAuth entities.
type ApicAuthQuery struct {
	config
	ctx        *QueryContext
	order      []apicauth.OrderOption
	inters     []Interceptor
	predicates []predicate.ApicAuth
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the ApicAuthQuery builder.
func (aaq *ApicAuthQuery) Where(ps ...predicate.ApicAuth) *ApicAuthQuery {
	aaq.predicates = append(aaq.predicates, ps...)
	return aaq
}

// Limit the number of records to be returned by this query.
func (aaq *ApicAuthQuery) Limit(limit int) *ApicAuthQuery {
	aaq.ctx.Limit = &limit
	return aaq
}

// Offset to start from.
func (aaq *ApicAuthQuery) Offset(offset int) *ApicAuthQuery {
	aaq.ctx.Offset = &offset
	return aaq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (aaq *ApicAuthQuery) Unique(unique bool) *ApicAuthQuery {
	aaq.ctx.Unique = &unique
	return aaq
}

// Order specifies how the records should be ordered.
func (aaq *ApicAuthQuery) Order(o ...apicauth.OrderOption) *ApicAuthQuery {
	aaq.order = append(aaq.order, o...)
	return aaq
}

// First returns the first ApicAuth entity from the query.
// Returns a *NotFoundError when no ApicAuth was found.
func (aaq *ApicAuthQuery) First(ctx context.Context) (*ApicAuth, error) {
	nodes, err := aaq.Limit(1).All(setContextOp(ctx, aaq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{apicauth.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (aaq *ApicAuthQuery) FirstX(ctx context.Context) *ApicAuth {
	node, err := aaq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first ApicAuth ID from the query.
// Returns a *NotFoundError when no ApicAuth ID was found.
func (aaq *ApicAuthQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = aaq.Limit(1).IDs(setContextOp(ctx, aaq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{apicauth.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (aaq *ApicAuthQuery) FirstIDX(ctx context.Context) int {
	id, err := aaq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single ApicAuth entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one ApicAuth entity is found.
// Returns a *NotFoundError when no ApicAuth entities are found.
func (aaq *ApicAuthQuery) Only(ctx context.Context) (*ApicAuth, error) {
	nodes, err := aaq.Limit(2).All(setContextOp(ctx, aaq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{apicauth.Label}
	default:
		return nil, &NotSingularError{apicauth.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (aaq *ApicAuthQuery) OnlyX(ctx context.Context) *ApicAuth {
	node, err := aaq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only ApicAuth ID in the query.
// Returns a *NotSingularError when more than one ApicAuth ID is found.
// Returns a *NotFoundError when no entities are found.
func (aaq *ApicAuthQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = aaq.Limit(2).IDs(setContextOp(ctx, aaq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{apicauth.Label}
	default:
		err = &NotSingularError{apicauth.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (aaq *ApicAuthQuery) OnlyIDX(ctx context.Context) int {
	id, err := aaq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of ApicAuths.
func (aaq *ApicAuthQuery) All(ctx context.Context) ([]*ApicAuth, error) {
	ctx = setContextOp(ctx, aaq.ctx, ent.OpQueryAll)
	if err := aaq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*ApicAuth, *ApicAuthQuery]()
	return withInterceptors[[]*ApicAuth](ctx, aaq, qr, aaq.inters)
}

// AllX is like All, but panics if an error occurs.
func (aaq *ApicAuthQuery) AllX(ctx context.Context) []*ApicAuth {
	nodes, err := aaq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of ApicAuth IDs.
func (aaq *ApicAuthQuery) IDs(ctx context.Context) (ids []int, err error) {
	if aaq.ctx.Unique == nil && aaq.path != nil {
		aaq.Unique(true)
	}
	ctx = setContextOp(ctx, aaq.ctx, ent.OpQueryIDs)
	if err = aaq.Select(apicauth.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (aaq *ApicAuthQuery) IDsX(ctx context.Context) []int {
	ids, err := aaq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (aaq *ApicAuthQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, aaq.ctx, ent.OpQueryCount)
	if err := aaq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, aaq, querierCount[*ApicAuthQuery](), aaq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (aaq *ApicAuthQuery) CountX(ctx context.Context) int {
	count, err := aaq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (aaq *ApicAuthQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, aaq.ctx, ent.OpQueryExist)
	switch _, err := aaq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (aaq *ApicAuthQuery) ExistX(ctx context.Context) bool {
	exist, err := aaq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the ApicAuthQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (aaq *ApicAuthQuery) Clone() *ApicAuthQuery {
	if aaq == nil {
		return nil
	}
	return &ApicAuthQuery{
		config:     aaq.config,
		ctx:        aaq.ctx.Clone(),
		order:      append([]apicauth.OrderOption{}, aaq.order...),
		inters:     append([]Interceptor{}, aaq.inters...),
		predicates: append([]predicate.ApicAuth{}, aaq.predicates...),
		// clone intermediate query.
		sql:  aaq.sql.Clone(),
		path: aaq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		Token string `json:"token,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.ApicAuth.Query().
//		GroupBy(apicauth.FieldToken).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (aaq *ApicAuthQuery) GroupBy(field string, fields ...string) *ApicAuthGroupBy {
	aaq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &ApicAuthGroupBy{build: aaq}
	grbuild.flds = &aaq.ctx.Fields
	grbuild.label = apicauth.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		Token string `json:"token,omitempty"`
//	}
//
//	client.ApicAuth.Query().
//		Select(apicauth.FieldToken).
//		Scan(ctx, &v)
func (aaq *ApicAuthQuery) Select(fields ...string) *ApicAuthSelect {
	aaq.ctx.Fields = append(aaq.ctx.Fields, fields...)
	sbuild := &ApicAuthSelect{ApicAuthQuery: aaq}
	sbuild.label = apicauth.Label
	sbuild.flds, sbuild.scan = &aaq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a ApicAuthSelect configured with the given aggregations.
func (aaq *ApicAuthQuery) Aggregate(fns ...AggregateFunc) *ApicAuthSelect {
	return aaq.Select().Aggregate(fns...)
}

func (aaq *ApicAuthQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range aaq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, aaq); err != nil {
				return err
			}
		}
	}
	for _, f := range aaq.ctx.Fields {
		if !apicauth.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if aaq.path != nil {
		prev, err := aaq.path(ctx)
		if err != nil {
			return err
		}
		aaq.sql = prev
	}
	return nil
}

func (aaq *ApicAuthQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*ApicAuth, error) {
	var (
		nodes = []*ApicAuth{}
		_spec = aaq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*ApicAuth).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &ApicAuth{config: aaq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, aaq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (aaq *ApicAuthQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := aaq.querySpec()
	_spec.Node.Columns = aaq.ctx.Fields
	if len(aaq.ctx.Fields) > 0 {
		_spec.Unique = aaq.ctx.Unique != nil && *aaq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, aaq.driver, _spec)
}

func (aaq *ApicAuthQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(apicauth.Table, apicauth.Columns, sqlgraph.NewFieldSpec(apicauth.FieldID, field.TypeInt))
	_spec.From = aaq.sql
	if unique := aaq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if aaq.path != nil {
		_spec.Unique = true
	}
	if fields := aaq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, apicauth.FieldID)
		for i := range fields {
			if fields[i] != apicauth.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := aaq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := aaq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := aaq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := aaq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (aaq *ApicAuthQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(aaq.driver.Dialect())
	t1 := builder.Table(apicauth.Table)
	columns := aaq.ctx.Fields
	if len(columns) == 0 {
		columns = apicauth.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if aaq.sql != nil {
		selector = aaq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if aaq.ctx.Unique != nil && *aaq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range aaq.predicates {
		p(selector)
	}
	for _, p := range aaq.order {
		p(selector)
	}
	if offset := aaq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := aaq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ApicAuthGroupBy is the group-by builder for ApicAuth entities.
type ApicAuthGroupBy struct {
	selector
	build *ApicAuthQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (aagb *ApicAuthGroupBy) Aggregate(fns ...AggregateFunc) *ApicAuthGroupBy {
	aagb.fns = append(aagb.fns, fns...)
	return aagb
}

// Scan applies the selector query and scans the result into the given value.
func (aagb *ApicAuthGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, aagb.build.ctx, ent.OpQueryGroupBy)
	if err := aagb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ApicAuthQuery, *ApicAuthGroupBy](ctx, aagb.build, aagb, aagb.build.inters, v)
}

func (aagb *ApicAuthGroupBy) sqlScan(ctx context.Context, root *ApicAuthQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(aagb.fns))
	for _, fn := range aagb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*aagb.flds)+len(aagb.fns))
		for _, f := range *aagb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*aagb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := aagb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// ApicAuthSelect is the builder for selecting fields of ApicAuth entities.
type ApicAuthSelect struct {
	*ApicAuthQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (aas *ApicAuthSelect) Aggregate(fns ...AggregateFunc) *ApicAuthSelect {
	aas.fns = append(aas.fns, fns...)
	return aas
}

// Scan applies the selector query and scans the result into the given value.
func (aas *ApicAuthSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, aas.ctx, ent.OpQuerySelect)
	if err := aas.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ApicAuthQuery, *ApicAuthSelect](ctx, aas.ApicAuthQuery, aas, aas.inters, v)
}

func (aas *ApicAuthSelect) sqlScan(ctx context.Context, root *ApicAuthQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(aas.fns))
	for _, fn := range aas.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*aas.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := aas.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
